[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thirdparty"
version = "0.1.0"
description = "ThirdParty build tool for O3DE"
requires-python = ">=3.11"
dependencies = [
    "packaging>=24.0",
    "patch-ng>=1.17.4",
    "colorama>=0.4",
    "jinja2>=3.0",
    "pygments>=2.0",
    "PyYAML>=6.0",
    "requests>=2.28",
    "python-dateutil>=2.8",
]

[project.optional-dependencies]
dev = [
    "pyright>=1.1.0",
    "libcst>=1.3",
]

[project.scripts]
thirdparty = "thirdparty.cli.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
