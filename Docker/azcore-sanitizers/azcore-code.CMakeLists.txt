# Copyright (c) Contributors to the Open 3D Engine Project.
# For complete copyright and license terms please see the LICENSE at the root of this distribution.
#
# SPDX-License-Identifier: Apache-2.0 OR MIT

# Keep this Docker-only source overlay limited to the production library, its
# test support library, and the executable used to load the test module.
add_subdirectory(Framework/AzCore)

if(LY_CLANG_SANITIZERS MATCHES "(^|,)memory(,|$)")
    if(NOT TARGET 3rdParty::Lua OR NOT DEFINED ENV{MSAN_LUA_ROOT})
        message(FATAL_ERROR "The AzCore MSan container requires its instrumented Lua runtime.")
    endif()
    set(msan_lua_root "$ENV{MSAN_LUA_ROOT}")
    set_target_properties(3rdParty::Lua PROPERTIES
        IMPORTED_LOCATION "${msan_lua_root}/lib/release/liblualib.a")
endif()

add_subdirectory(Framework/AzTest)
add_subdirectory(Tools/AzTestRunner)

# Root post-processing calls this even when no launchers are registered.
function(ly_delayed_generate_static_modules_inl)
endfunction()
