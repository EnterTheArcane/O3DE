# Jolt 5.6 feature coverage

This document is the authoritative feature inventory and qualification ledger for the standalone Jolt Gem. It describes the current
working tree; it is not a roadmap-completion claim. Native Jolt remains a private implementation detail built by `JoltNative.cmake`.

## Classification contract

Every Jolt 5.6 subsystem is assigned exactly one classification:

- **Publicly exposed**: an AZ-facing API exists without public `JPH` types.
- **Used privately**: native functionality implements an exposed feature but is intentionally hidden behind the Gem boundary.
- **Replaced by engine facility**: an engine service owns the corresponding responsibility.
- **Upstream limitation**: Jolt 5.6 cannot provide the capability without upstream work or a maintained native extension.
- **Deferred platform integration**: the design is intentionally excluded until a platform owner can qualify it.
- **Missing**: the native capability should be exposed, but the required Gem surface or proof is incomplete.

For a **Publicly exposed** row, `C++ and authoring proof` names current evidence and `Scenario and performance proof` names current or
required evidence. `Required` is an open qualification gate, not evidence. `N/A` means the subsystem has no meaningful component,
serialization, scenario, or benchmark surface; it never means that applicable proof was omitted.

## Current qualification snapshot

| Area | Current evidence | Remaining gate |
|---|---|---|
| Private dependency | JoltPhysics 5.6.0 is pinned and patched privately through `3rdParty/JoltNative.cmake`. Native targets, headers, and ABI definitions are not installed or published through a find module. | Revalidate pristine fetch, cached refresh, installed modular, and installed monolithic paths during Phase 6. |
| Native API boundary | Public-header isolation compiles every file in `Code/Include/Jolt` independently. Public headers contain no `JPH` declaration or include. | Repeat with unity disabled, AppleClang, clang-cl, double precision, and the installed public consumer. |
| Windows CPU checkpoint | MSVC Profile and Release pass 222 runtime, editor, and builder tests in one host-tools suite after compiling every public header in isolation. Profile `Editor` and `AssetProcessor` build, the CPU Hair scenario passes, and generated Jolt targets have no Atom or GPU-backend dependency. | Rebuild all evidence after the later implementation phases and perform the Phase-6 clean-room run. |
| Linux checkpoint | A pre-Phase-0 Clang 20/Ninja Profile run passed 175 runtime tests and public-header isolation. GCC reached engine code but was blocked before Jolt by existing AzCore warnings promoted to errors. | Linux is deliberately deferred until Windows stabilizes; the CPU-only Phase-0 tree has not been requalified there. |
| Apple CPU | No current hardware evidence. | Follow `APPLE_QUALIFICATION.md`; macOS is an external qualification gate. |
| Android, iOS, and WebAssembly | No current device/toolchain evidence. | Dedicated platform qualification remains external. |
| AutomatedTesting | Six null-renderer scenarios cover dynamically authored and saved components, CPU Hair, advanced components, queries, diagnostics, and rollback. | Replace coarse coverage with the focused Phase-4 scenarios, feature gallery, and deterministic stress/soak level. |
| Performance | Current Windows Release matched-provider artifacts and focused lifecycle, query, rollback, soft-body, ragdoll, multi-world, and CPU Hair benchmarks are documented in `PERFORMANCE.md`. | Expand absolute workloads, allocations, memory, integrity checks, and 1/4/8-worker scaling in Phase 5. |

## Native foundation and integration inventory

| Jolt 5.6 subsystem | Classification | AZ-facing boundary or rationale | C++ and authoring proof | Scenario and performance proof |
|---|---|---|---|---|
| Source, build options, and ABI | Used privately | `JoltNative.cmake` owns FetchContent, the private patch, deterministic defines, precision, SIMD, profiling, statistics, and CPU compute. `RuntimeInfo` exposes only fingerprinted build facts. | `NativeRuntimeTests.ReportsPinnedDeterministicConfiguration`; `SimulationTests.RuntimeInfoReportsDeterministicCpuHair`; manifest and header-isolation checks. | Existing runtime scenarios inspect build information. Required: Phase-6 source/install matrix. |
| Core allocation, reference counting, RTTI, factories, hashing, and streams | Used privately | Native resource ownership and binary readers stay in `Source/Jolt`; AZ allocators, handles, `AZ::TypeId`, and streams define the public contract. | Resource lifetime, stale-generation, archive corruption, and slot-reuse tests throughout `SimulationTests`; no component applies. | Required: Phase-5 persistent/temporary allocation and slot-reuse benchmarks. |
| Math, geometry primitives, and native transforms | Used privately | Conversion code maps between `JPH` math and `AZ::Vector*`, `AZ::Quaternion`, `AZ::Transform`, bounds, and uniform scale. | `NativeRuntimeTests.DeterministicFloatScopeRestoresExceptionFlags`; shape, query, large-origin, and determinism tests. | Matched step/query benchmarks. Required: Phase-5 conversion and assembly report. |
| Job system and barriers | Replaced by engine facility | Native jobs are adapted to `AZ::JobContext`; deterministic float state is established on each calling and worker thread. | `SimulationTests.TwoThreadWorldUsesOneBackgroundJobWorker`, `AutomaticSingleWorkerWorldsStepConcurrently`, batch-query and CPU-Hair worker float tests. | `Jolt/Diagnostic/Step/AutomaticWorlds`; required 1/4/8-worker stress scaling. |
| Native tracing and profiler | Replaced by engine facility | `AZ_PROFILE_SCOPE` and engine diagnostics own public profiling; the optional external native profiler feeds private phase data. | Configuration/runtime-info tests; no serialized surface. | Profile captures in `PERFORMANCE.md`. Required: Phase-5 scope-presence audit. |
| Assertions and trace output | Replaced by engine facility | Native callbacks route failures and trace text into AZ diagnostics without exposing callback types. | Initialization and invalid-configuration tests; no authoring surface. | Required: Phase-4 failure scenarios and Phase-6 warning audit. |
| ObjectStream and binary serialization | Used privately | Native binary state is wrapped in fingerprinted, bounded AZ archives for shapes, scenes, skeletons, animations, soft bodies, and rollback. Compiled assets add a private versioned envelope with an exact payload size and integrity hash. | `CookedShapeArchivesRoundTripMaterialsAndRejectMismatchedData`, `SkeletonAndAnimationArchivesRestoreValidatedNativeResources`, `WorldSnapshotArchivesRoundTripAndRejectCorruptionAtomically`; `EditorAssetBuilderTests` reject truncated, corrupt, unsupported-version, and invalid source/product data without publishing or partially replacing an asset. | `Jolt/Lifecycle/ImportSoftBodyDefinition`; required asset-load and corruption benchmarks. |
| AABB trees, convex-hull builder, triangle splitting, and geometry utilities | Used privately | Cooking, mesh optimization, supporting faces, broadphase, and editor debug geometry use these internals; callers provide AZ data. | `ConvexHullTopologyExposesCenterOfMassRelativeNativeGeometry`, cooking rejection, mesh/heightfield, and scene tests; collider editor tests. | Shape cooking, overlap, raycast, and broadphase benchmarks; more required in Phase 5. |
| CPU compute shaders | Used privately | `JPH::ComputeSystemCPU` is lazily owned by the runtime for deterministic CPU Hair. | CPU Hair creation, update, readback, snapshot, and 1/4/8-worker tests; Hair editor export tests. | `Jolt_CpuHair`; `Jolt/Hair/Update` and `Jolt/Hair/Readback`. |
| DX12, Vulkan, and Metal compute backends | Deferred platform integration | Experimental native GPU sources remain in the fetched checkout but are not built or wrapped. No Atom, RHI, shader, or GPU-handle API belongs to the Gem. | Build-manifest and native-dependency checks prove absence; no component or serialization surface exists. | N/A until a stable cross-system GPU ownership contract is approved. |
| Native debug renderer | Used privately | The adapter converts native lines, triangles, text, and geometry into `IDebugDraw` and bounded `DebugCapture` data. | `DiagnosticDrawingBatchesGeometryFiltersBodiesAndRestoresWorldCoordinates`; `DebugRendererTests` primitive and canonical replay tests. | Current advanced/query scenarios; required `Jolt_Diagnostics` and debug-capture benchmarks. |
| Jolt TestFramework, Viewer, samples, and application loop | Replaced by engine facility | Components, Editor, AutomatedTesting, Asset Processor, AzTest, and Google Benchmark own these responsibilities. | Module/component/builder tests and six current scenarios. | Required: Phase-4 gallery and stress levels. |

## Collision, shapes, and query inventory

| Jolt 5.6 subsystem | Classification | Public API and component | C++ and authoring proof | Scenario and performance proof |
|---|---|---|---|---|
| Materials, density, friction, restitution, and combine policy | Publicly exposed | `Material.h`, `ShapeConfiguration.h`, `ISystem`; authored by `ColliderComponent`. | `WorldShapesExposeNativePropertiesAndSurfaceData`, `MaterialCombineModesControlInitialContactSettings`, material lifetime tests; collider serialization/editor tests. | Existing feature/query scenarios. Required: `Jolt_ShapesAndCooking` and material-heavy memory tests. |
| Object layers, broadphase layers, collision groups, and group filters | Publicly exposed | `Collision.h`, `SystemConfiguration.h`, `ISystem`; world configuration and body/collider components. | `ConfiguredObjectLayerTableControlsCollisionPairs`, `GroupFilterTableControlsBodyPairsAndOwnsItsLifetime`, callback-state rollback tests. | Existing world-query scenario. Required: `Jolt_EventsAndFilters` and filter-cost benchmarks. |
| Box, sphere, capsule, cylinder, tapered capsule, tapered cylinder, plane, triangle, and empty shapes | Publicly exposed | `ShapeConfiguration.h`, `Shape.h`, `ICooking`, `ISystem`; `ColliderComponent`. | `CreatesEveryAnalyticAndConvexPrimitive`, exact native state and editor collider manipulation tests; tagged-variant JSON round-trip. | Existing component/feature scenarios. Required: `Jolt_ShapesAndCooking`; matched query benchmarks cover boxes and spheres. |
| Convex hull and mesh shapes | Publicly exposed | `ConvexHullShapeConfiguration`, `MeshShapeConfiguration`, cooking/archive APIs; `ColliderComponent`. | Hull topology, invalid geometry, cooked archive, mesh metadata, editor bounds/manipulator tests. | Existing feature scenario. Required: shape cooking and topology benchmarks. |
| Heightfields | Publicly exposed | `HeightfieldShapeConfiguration`, inspection/mutation APIs and bus; `ColliderComponent`. | `HeightfieldUsesPositiveYRowsAndZUpHeights`, runtime mutation and script-safe component tests; editor serialization. | Existing feature scenario. Required: `Jolt_ShapesAndCooking`, update and memory benchmarks. |
| Static and mutable compounds | Publicly exposed | `CompoundShapeConfiguration`, child identity/mutation APIs; `ColliderComponent`. | `CookedCompositionsRetainChildrenAndInstantiateAsOneShape`, `MutableCompoundEditsPreserveBodyAndDependencyIdentity`, archive remapping tests. | Existing advanced scenario. `Jolt/Lifecycle/CreateDestroyCookedCompounds`; more Phase-5 churn coverage required. |
| Rotated/translated, scaled, and center-of-mass-offset decorated shapes | Publicly exposed | `DecoratedShapeConfiguration`, standalone clone/scale APIs; `ColliderComponent`. | Composed-shape ownership, mutable clone, mass/center-of-mass, query-scale, and editor offset-manipulator tests. | Existing advanced/query scenarios. Required: decorated-shape query and update benchmarks. |
| Shape metadata, subshape traversal, triangles, supporting faces, bounds, and surface projection | Publicly exposed | `Shape.h`, `Query.h`, caller-owned spans and fixed-stride face buffers; collider/query buses. | `GeometryQueriesReturnSupportingFacesAndWorldTrianglesWithoutAllocatingOutputs`, shape metadata, heightfield projection, and collider script tests. | Existing query scenario. Required: `Jolt_Queries` and bounded-output benchmarks. |
| Binary cooking, immutable sharing, cloning, and restoration | Publicly exposed | `Cooking.h`, cooked handles/archives, scene dependencies; no dedicated runtime component. | Cooking rejection, cross-world sharing, archive checksum/fingerprint, compound dependency, and transactional scene tests. | Existing advanced/saved scenarios. Required: `Jolt_ShapesAndCooking`, allocation and load benchmarks. |
| Custom convex shapes through native `UserConvex1` | Publicly exposed | `CustomShape.h`, `ICustomConvexShapeProvider`, cooked provider identity; authored through collider custom-convex data. | `CustomConvexShapesCookOnceAndRemainNativeAtRuntime`, editor bounds, serialization and lifetime tests. | Existing advanced scenario. Required: custom-convex benchmarks. |
| Arbitrary custom shapes and collision dispatch | Publicly exposed | `CustomShape.h`, `ICustomShapeProvider`, immutable proxy/runtime data, AZ-facing collision views and collectors; one private `User1` adapter dispatches unlimited logical `AZ::TypeId` providers. `ColliderComponent` authors provider data and bounds. | `CustomShapesRetainProvidersAcrossCookingArchivesAndRuntimeUse` covers convex/mesh proxies, both dispatch directions, custom/custom, invalid output, concurrent deterministic dispatch with restored float state, materials/user data, dependencies, archives, snapshots, missing providers, and in-use removal. `CrossModuleProviderTests.ProvidersRetainModuleResourcesAndReloadWithoutStaleState` proves dynamic registration, retained lifetime, unload/reload, archived import, and stale-resource rejection. Tagged JSON and editor-bounds tests cover authoring. | Required: `Jolt_ShapesAndCooking`, `Jolt_Queries`, provider failure scenarios, and custom-dispatch/cooking benchmarks. |
| Broadphase ray, box, sphere, and oriented-box casts | Publicly exposed | `Query.h`, `ISystem`, `WorldQueryBus`; no component. | `BroadPhaseQueriesExposeAllNativeGeometryModes`, concurrency and simulation-overlap tests. | Existing world-query scenario. `Jolt/Diagnostic/RaycastBroadPhaseGrid`; required `Jolt_Queries`. |
| Narrowphase raycasts: closest, any, all, closest-per-body, and batches | Publicly exposed | `Query.h`, `ISystem`, `WorldQueryBus`; caller-owned buffers and deterministic ordering. | Closest identity/surface, per-body, exact ties, concurrent callers, worker float-state, overflow/reflection tests. | Existing world-query scenario. Matched scalar/batch raycast benchmarks. |
| Point and shape overlaps | Publicly exposed | `Query.h`, `Shape.h`, `ISystem`, `WorldQueryBus`; filters and bounded collectors. | Shape identity, scale/supporting-face, stable overflow order, optimized-sphere equivalence, and standalone point tests. | Existing world-query scenario. Matched sphere-overlap and diagnostic count-only benchmarks. |
| Shape casts and backface/active-edge policy | Publicly exposed | `Query.h`, `ISystem`, `WorldQueryBus`; closest-per-body and bounded all-hit modes. | Shape overlap/cast identity, scale, supporting faces, broadphase modes, deterministic tie tests. | Existing world-query scenario. Required: `Jolt_Queries` and shape-cast benchmarks. |
| Retained transformed-shape versus transformed-shape collision and cast | Publicly exposed | `TransformedShape` is a world-owned dependency lease; `Query.h` and `ISystem` expose retained and immediate placement overloads, canonical caller spans, streaming collectors, filters, faces, identities, materials, user data, uniform scale, and early-out tightening. | `GeometryQueriesReturnSupportingFacesAndWorldTrianglesWithoutAllocatingOutputs` covers collision/cast, convenience placement, body-independent lifetime, faces, identity, scale, stale generation and result-buffer preservation, cross-world/system rejection, leased shape/world destruction, concurrent deterministic calls, and caller float-state restoration. Component-bus proof covers safe forced system shutdown; custom-shape proof covers collector early-out and dispatch directions. | Required: `Jolt_Queries`, failure scenarios, pair-query and lease-lifetime benchmarks. |
| Built-in collision dispatch tables and narrowphase algorithms | Used privately | Native dispatch handles built-in and provider-adapted shape pairs behind public world/shape queries and simulation; native registrations are not exposed. | Built-in shape-pair, query, contact, custom-provider, and determinism tests. | Matched query/step benchmarks; required custom-dispatch benchmarks. |
| Soft-body versus rigid/soft collision and manifold generation | Publicly exposed | `SoftBody.h`, `ISoftBodyContactCallbacks`, soft-body APIs/component. | Rejection, mass scaling, manifold, multiple sensor, and linear-cast callback tests; non-default serialization/editor tests. | Existing advanced scenario. Required: `Jolt_SoftBodies`, contact-heavy and soft-collision benchmarks. |

## Dynamics and high-level simulation inventory

| Jolt 5.6 subsystem | Classification | Public API and component | C++ and authoring proof | Scenario and performance proof |
|---|---|---|---|---|
| Physics system, worlds, update phases, origin shifting, and broadphase maintenance | Publicly exposed | `System.h`, `Simulation.h`, `SystemConfiguration.h`, world handles and notifications; system component owns ticking. | World lifecycle/configuration, fixed catch-up, origin, automatic concurrency, update callback, and runtime-info tests. | Existing world-query/advanced scenarios. Matched step and automatic-world benchmarks; stress origin-shift gate required. |
| Bodies, body interfaces, locks, lifecycle, and simulation membership | Publicly exposed | `BodyConfiguration.h`, `BodyBus.h`, `RigidBodyBus.h`, `StaticRigidBodyBus.h`, `ISystem`; rigid/static components. | Full configuration, bulk atomic operations, stale/cross-world handles, membership, destruction, slot reuse, transform synchronization, and serialization/editor tests. | Current component/feature scenarios. Matched lifecycle and step benchmarks; topology-churn gate required. |
| Motion properties, mass/inertia, forces, impulses, kinematics, damping, DOFs, sensors, and CCD | Publicly exposed | Body and rigid-body APIs/configurations; `RigidBodyComponent`. | Complete configuration, mass response, force/reset, kinematic, sensor, motion-quality, contact invalidation, and editor export tests. | Existing feature scenario. Required: events/filters and stress scenarios plus CCD, sleep/wake, and force benchmarks. |
| Islands, sleeping, activation, deactivation, and body-move publication | Publicly exposed | World/body configuration, event subscriptions, diagnostics; body components. | Sleep/wake state, membership, activation and stable move-event tests. | Existing feature scenario. Required: `Jolt_EventsAndFilters`, sleep/wake-storm benchmark and stress phase. |
| Contact creation, validation, mutation, caching, and lifecycle events | Publicly exposed | `BodyCollision.h`, `Event.h`, contact callback/notification interfaces; collider and body components. | Contact validation/rejection/mutation, combine modes, destruction identity, event configuration, callback rollback, and serialization/reflection tests. | Existing advanced/query scenarios. Required: `Jolt_EventsAndFilters`, event-storm and contact-cache benchmarks. |
| Body-pair collision override, simulation shape filters, and step listeners | Publicly exposed | `IBodyPairCollider`, `ISimulationShapeFilter`, `IStepListener`; C++-only synchronous callbacks. | Every collision mode, custom hits, invalid output, provider identity, no-lock step mutation, rollback participant tests. | Required: cross-module test, `Jolt_EventsAndFilters`, callback and failure benchmarks. |
| Standard constraints and motors | Publicly exposed | `Constraint.h`, `ConstraintBus.h`, configuration component for cone, distance, fixed, gear, hinge, path, point, pulley, rack-and-pinion, six-DOF, slider, and swing-twist. | Creation, measurement, mutation, warm-start reset, limits/motors/breakage, callback, debug draw, tagged JSON, and editor geometry tests. | Existing feature/advanced scenarios. Required: `Jolt_Constraints` and per-category benchmarks. |
| Custom constraints through native `User1` | Publicly exposed | `CustomConstraint.h`, `ICustomConstraintProvider`; C++ registration with AZ scalar Jacobian rows. | `CustomConstraintsBatchRowsAndRetainProvidersUntilDestruction`, snapshot/provider lifetime tests, and `CrossModuleProviderTests.ProvidersRetainModuleResourcesAndReloadWithoutStaleState`. | Required: `Jolt_Constraints`, missing-provider failures, and custom-constraint benchmarks. |
| Hermite and custom paths | Publicly exposed | `Path.h`, `PathBus.h`, `ICustomPathProvider`; path component and path constraints. | Deterministic custom sampling/lifetime, component ownership, tagged JSON, editor curve/bounds tests, and `CrossModuleProviderTests.ProvidersRetainModuleResourcesAndReloadWithoutStaleState`. | Existing feature scenario. Required: `Jolt_Constraints` and path benchmarks. |
| Physical characters | Publicly exposed | `Character.h`, `CharacterBus.h`, character configuration/component; backing body identity stays private. | Stairs, floor adhesion, shape switching, contacts, ground refresh, metadata and editor visualization/export tests. | Existing advanced scenario. Required: `Jolt_Characters` and locomotion benchmarks. |
| Virtual characters and character-to-character collision | Publicly exposed | `Character.h`, `VirtualCharacterBus.h`, virtual-character component. | Handle/state, inner body, deterministic collision checks, locomotion/contact, reflection and editor guide tests. | Existing advanced scenario. Required: `Jolt_Characters` and virtual-character benchmarks. |
| Vehicle constraint and wheeled controller | Publicly exposed | `Vehicle.h`, `VehicleBus.h`, wheeled vehicle component; engine, transmission, differentials, anti-roll bars, wheels, and tire callbacks. | Lifecycle, bounded state, configuration, callbacks, serialization, editor assembly tests. | Existing advanced scenario. Required: `Jolt_Vehicles`, callback module proof, and controller benchmarks. |
| Motorcycle controller | Publicly exposed | `Vehicle.h`, `VehicleBus.h`, motorcycle component. | Specialized runtime component, reflection/serialization, and editor default visualization tests. | Existing advanced scenario. Required: `Jolt_Vehicles` and motorcycle benchmarks. |
| Tracked vehicle controller | Publicly exposed | `Vehicle.h`, `VehicleBus.h`, tracked vehicle component; tracks and controller state. | Specialized component, configuration, serialization, and closed-track editor visualization tests. | Existing advanced scenario. Required: `Jolt_Vehicles` and tracked-controller benchmarks. |
| Ray, sphere, and cylinder vehicle collision testers | Publicly exposed | Vehicle collision-test configuration and runtime state through AZ types; vehicle components. | Vehicle configuration/runtime tests cover every tester variant and bounded wheel contacts. | Existing advanced scenario. Required: `Jolt_Vehicles` and per-tester benchmarks. |
| Skeletons, poses, animations, and skeleton mappers | Publicly exposed | `Skeleton.h`, `SkeletonBus.h`, `SkeletonAsset.h`, skeleton component; `.jolt.json` source to `.jolt` product. | Mapping, allocation-free pose sampling, native archive, component reload, non-default builder and editor export tests. | Current saved/advanced scenarios. Required: `Jolt_RagdollsAndSkeletons` and sampling/mapping benchmarks. |
| Ragdoll definitions, instances, drive, and simulation membership | Publicly exposed | `Ragdoll.h`, `RagdollBus.h`, ragdoll component. | Child ownership, poses, motors, drive modes, snapshots, worker determinism, serialization, editor hierarchy tests. | Existing advanced scenario. Required: `Jolt_RagdollsAndSkeletons`; membership/recreate benchmarks already exist. |
| Soft-body definitions, constraints, rods, skinning, contacts, runtime controls, and bodies | Publicly exposed | `SoftBody.h`, `SoftBodyBus.h`, soft-body component; scene assets can own cooked definitions. | Definition/archive, force/velocity, rods, blended inverse binds, constraints/backstops, controls, contacts, sensors, rollback and 1/4/8-worker tests; non-default editor/serialization proof. | Existing advanced scenario. Required: `Jolt_SoftBodies`; definition creation/import benchmarks exist, simulation/skinning benchmarks required. |
| CPU Hair definitions, constraints, skinning, update, readback, and state | Publicly exposed | `Hair.h`, `HairBus.h`, Hair component; CPU compute remains private. | Shared definition/state buffers, component ownership/scale, auto/manual update, readback, snapshot/digest and exact 1/4/8-worker tests; editor strands/export tests. | `Jolt_CpuHair`; `Jolt/Hair/Update` and `Jolt/Hair/Readback`; required `Jolt_Hair` and stress phase. |
| State recording, body snapshots, filtered/multipart rollback, archives, digests, and replay | Publicly exposed | `Rollback.h`, `Diagnostics.h`, `ISystem`, script-safe world buses; no authoring component. | Transactional recapture/restore, exact consumption, archive corruption, multipart identity, callback participants, partitions, allocation reuse, soft body/Hair/event replay, and worker digest tests. | Existing world-query scenario. Required: `Jolt_RollbackAndDeterminism`, soak replay; rollback benchmarks already exist. |
| Network transport and cross-build snapshot interchange | Upstream limitation | Jolt state streams are same-build implementation state. The Gem's archive intentionally validates the exact native/ABI fingerprint and does not promise cross-version or cross-platform interchange. | Same-build cross-world archive and mismatch rejection tests; no component applies. | N/A. Network protocols must serialize game-owned state and negotiate their own schema. |
| Scene definitions, ownership, instantiation, and assets | Publicly exposed | `Scene.h`, `SceneAsset.h`, `SceneBus.h`, scene component; `.jolt.json` source to typed `.jolt` product. | Dependency graphs, mixed bodies/constraints, transaction rollback, reload preservation, archive fingerprint, builder and editor export tests. | Current saved scenario. Required: `Jolt_ScenesAndAssets`, feature gallery, load/instantiate benchmarks. |

## Diagnostics and tooling inventory

| Jolt 5.6 subsystem | Classification | Public API and component | C++ and authoring proof | Scenario and performance proof |
|---|---|---|---|---|
| Runtime statistics and resource capacities | Publicly exposed | `Diagnostics.h`, `SystemConfiguration.h`, `ISystem`; optional native body simulation counters are reported through AZ types. | Runtime-info/configuration/statistics and capacity-reuse tests; no component applies. | Existing advanced/query scenarios. Required: Phase-5 resettable memory/resource/job/lock/query/event/snapshot statistics. |
| Native broadphase statistics | Publicly exposed | Private `LY_JOLT_ENABLE_BROADPHASE_STATISTICS` controls native collection; `Diagnostics.h` and `ISystem` expose deterministic caller-buffer snapshot/reset by query kind and layer. Disabled builds return `Unavailable`. | `NativeDiagnosticStatisticsReportAvailabilityCapacityAndReset`; Windows Profile disabled, broad-only, narrow-only, and combined build matrix passed on 2026-08-15. | Required: `Jolt_Diagnostics`, broadphase workloads and statistics-overhead benchmark. |
| Native narrowphase statistics | Publicly exposed | Private `LY_JOLT_ENABLE_NARROWPHASE_STATISTICS` controls native collection; `Diagnostics.h` and `ISystem` expose process-wide caller-buffer snapshot/reset by query and shape-kind pair. Disabled builds return `Unavailable`. | `NativeDiagnosticStatisticsReportAvailabilityCapacityAndReset`; Windows Profile disabled, broad-only, narrow-only, and combined build matrix passed on 2026-08-15. | Required: `Jolt_Diagnostics`, contact workloads and statistics-overhead benchmark. |
| Detailed native phase profiling | Publicly exposed | Private native callbacks feed existing profiling scopes when `LY_JOLT_ENABLE_DETAILED_PROFILING` is enabled; `RuntimeInfo` reports availability. | Configuration/runtime-info tests; no serialized component. | Current Profile captures. Required: Phase-5 scope placement and overhead audit. |
| Debug drawing and bounded deterministic capture | Publicly exposed | `DebugDraw.h`, `Diagnostics.h`, `ISystem`; Editor components visualize authored geometry without native renderer types. | Primitive coverage, canonical replay, world-coordinate restoration, per-world isolation, catch-up accumulation, direct character/buoyancy tests. | Existing advanced/query scenarios. Required: `Jolt_Diagnostics`, full feature capture and bounded-memory benchmark. |
| Physics scene and skeleton Asset Builder | Publicly exposed | `Jolt::Editor::AssetBuilder` recognizes `*.jolt.json` by stable `ClassName` and emits typed `.jolt` products; native archives stay private. The product envelope validates version, exact size, and payload integrity before handlers replace live data. | Seven `EditorAssetBuilderTests` cover registration fingerprint, job policy, malformed/truncated JSON, unsupported classes, dependencies, stable sub-IDs, non-default custom/soft-body/skinning/skeleton data, missing providers, corrupt/truncated/unsupported products, and transactional load. Runtime tests cover asset load and component reload. | Current saved scenario. Required: installed consumption and `Jolt_ScenesAndAssets`; load/cooking performance moves to Phase 5. |
| BehaviorContext and Python buses | Publicly exposed | Script-safe values, handles, enums, requests, notifications, bounded collections, diagnostics, skeleton resources, queries, and rollback are reflected; synchronous callbacks stay C++-only. | Complete bus/type/enum reflection tests and component bus tests. | Six current scripts. Required: Phase-4 focused scenarios and reflection parity audit. |
| Editor authoring, component modes, visualization, and selection | Publicly exposed | Editor wrappers exist for collider, rigid/static body, constraints, physical/virtual characters, vehicles, soft bodies, ragdolls, skeletons, paths, scenes, and CPU Hair. | Forty-two editor tests cover runtime export, meaningful geometry/manipulators, full-module reflection, non-default binary round-trip for every authorable component, representative undo/redo, and every-component prefab copy and save/reload. `EveryAuthorableConfigurationRoundTripsNonDefaultJson` covers every authorable configuration supported by JSON. | Current component/saved scenarios. Required: Phase-4 gallery and focused scenarios; broader interactive editor automation remains a qualification task. |
| Cross-module callback/provider ownership | Publicly exposed | Public custom-shape, collision-dispatch, custom-constraint, and custom-path interfaces are consumed by a separately loaded `Jolt.TestProviders` module without exposing native types. | `CrossModuleProviderTests.ProvidersRetainModuleResourcesAndReloadWithoutStaleState` proves symbol lookup, registration, use across cooking/query/step, in-use rejection, ordered destruction, unregistration, unload, reload, archive restoration, and stale-resource rejection. | Required: Phase-4 missing-provider scenarios and Phase-5 provider lifecycle benchmarks. |

## Authorable component evidence matrix

Every row below is included in `EditorComponentTests.EveryAuthorableComponentPreservesNonDefaultConfiguration`,
`ConfigurationSerializerTests.EveryAuthorableConfigurationRoundTripsNonDefaultJson`, and
`PrefabComponentTests.EveryAuthorableComponentSurvivesPrefabCopyAndSaveReload`. The binary and JSON tests use representative
non-default nested data and asset references; the prefab test verifies copied and reloaded editor components still export the intended
runtime type. `PrefabComponentTests.NonDefaultRigidBodySupportsPrefabUndoRedoAndSaveReload` separately proves the common prefab undo,
redo, propagation, save, removal, and reload machinery with non-default data.

| Component | Runtime API/bus | Current serialization/editor evidence | Current AutomatedTesting | Required focused scenario |
|---|---|---|---|---|
| Collider | `ColliderBus`, shape and material APIs | Collider metadata round-trip; primitive manipulators; compound offset; custom-convex and general custom bounds | `Jolt_ComponentSmoke`, `Jolt_FeatureComponents` | `Jolt_ShapesAndCooking` |
| Rigid body | `RigidBodyBus`, `BodyBus` | Runtime export and non-default body configuration | `Jolt_ComponentSmoke`, `Jolt_FeatureComponents` | `Jolt_EventsAndFilters` |
| Static rigid body | `StaticRigidBodyBus`, `BodyBus` | Runtime export and collider visualization | `Jolt_ComponentSmoke`, `Jolt_SavedComponents` | `Jolt_ShapesAndCooking` |
| Constraint | `ConstraintBus` | Tagged geometry round-trip; every authored frame/limit geometry | `Jolt_FeatureComponents` | `Jolt_Constraints` |
| Character | `CharacterBus` | Runtime export; slope/support guides and selection | `Jolt_AdvancedComponents` | `Jolt_Characters` |
| Virtual character | `VirtualCharacterBus` | Runtime export; shape, offset, and step guides | `Jolt_AdvancedComponents` | `Jolt_Characters` |
| Wheeled vehicle | `VehicleBus` | Specialized export; wheel assembly visualization | `Jolt_AdvancedComponents` | `Jolt_Vehicles` |
| Motorcycle | `VehicleBus` | Specialized export and usable authored defaults | `Jolt_AdvancedComponents` | `Jolt_Vehicles` |
| Tracked vehicle | `VehicleBus` | Specialized export and closed track routes | `Jolt_AdvancedComponents` | `Jolt_Vehicles` |
| Soft body | `SoftBodyBus` | Non-empty skin data; faces, joints, distances, and backstops | `Jolt_AdvancedComponents` | `Jolt_SoftBodies` |
| Ragdoll | `RagdollBus` | Runtime export; hierarchy and part selection | `Jolt_AdvancedComponents` | `Jolt_RagdollsAndSkeletons` |
| Skeleton | `SkeletonBus`, `SkeletonComponentBus` | Asset/runtime export and transactional reload | `Jolt_AdvancedComponents`, `Jolt_SavedComponents` | `Jolt_RagdollsAndSkeletons` |
| Path | `PathBus` | Runtime export; visible Hermite curve and bounds | `Jolt_FeatureComponents` | `Jolt_Constraints` |
| Physics scene | `SceneBus` | Asset/runtime export and transactional reload | `Jolt_SavedComponents` | `Jolt_ScenesAndAssets` |
| CPU Hair | `HairBus` | Runtime export; strand visualization and selection | `Jolt_CpuHair` | `Jolt_Hair` |

## Qualification rules

- Keep this Markdown human-maintained. Do not add a companion schema or generate this file.
- The Phase-6 validation entry point will fail when a Windows row remains **Missing**, or when a **Publicly exposed** row has no named
  C++ proof and no explicit scenario/performance disposition.
- A passing aggregate target does not qualify a row. Applicable success, failure, stale-handle, mutation, concurrency, determinism,
  snapshot, serialization, editor, script, allocation, and performance axes must be named explicitly.
- Deterministic compilation, precise floating-point state, no contraction, worker scopes, canonical query/event ordering, callback-state
  fingerprints, and snapshot revision checks are mandatory shipping defaults.
- Optional simulation or diagnostic counters must pass in enabled and disabled builds. Disabled APIs return an explicit unavailable
  result; they never fabricate zero measurements.
- Release performance artifacts require at least 30 repetitions, matching workload signatures and policies, raw samples, median parity,
  bootstrap and tail bounds, and coefficient of variation at most 5%. Profile captures are diagnostic only.
- Assembly inspection is diagnostic and must report stack frames, out-of-line calls, branches, conversions, vectorization, copies, and
  inlining boundaries for selected hot functions. Do not gate exact opcodes.
- Linux runs remain deferred until Windows stabilizes. macOS follows `APPLE_QUALIFICATION.md`; iOS and other device platforms remain
  separately reported external gates.
